name: dart_query
description: TanStack Query for Dart. Automatic caching, stale-while-revalidate, background refetching, mutations, infinite queries.
version: 0.1.0
homepage: https://github.com/AhmedAbouelworworoud/dart_query

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.9.0

dev_dependencies:
  test: ^1.24.0
  fake_async: ^1.3.0

resolution: workspace
